"""Billing domain models stub.

TODO: Define ORM-backed billing models (Subscription, Plan, Invoice, etc.)
"""

from __future__ import annotations

# TODO: Implement billing models
# Example:
# from tortoise import fields, models
#
# class Plan(models.Model):
#     id = fields.UUIDField(pk=True)
#     name = fields.CharField(max_length=100)
#     price_monthly = fields.DecimalField(max_digits=10, decimal_places=2)
#
# class Subscription(models.Model):
#     id = fields.UUIDField(pk=True)
#     plan = fields.ForeignKeyField("models.Plan", related_name="subscriptions")
#     status = fields.CharField(max_length=50)
#     created_at = fields.DatetimeField(auto_now_add=True)
