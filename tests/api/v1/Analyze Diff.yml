info:
  name: Analyze Diff
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/v1/analyze"
  body:
    type: text
    data: "{{Polar}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
