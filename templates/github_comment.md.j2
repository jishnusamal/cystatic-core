## Factor — What to validate before merging

*Factor traces what a PR touches and tells you what to validate before merging.*

Factor identified **{{ findings | length }} {{ "area" if findings | length == 1 else "areas" }} worth validating before merge.

{% for finding in findings %}
### 🔍 {{ loop.index }}. {{ finding.title }}

**{{ finding.validation }}**

Specifically:

{% for step in finding.steps %}
{{ loop.index }}. {{ step }}
{% endfor %}

**Why this is worth checking:**

{{ finding.why }}

**Evidence:** {{ finding.evidence }}

**Confidence:** {{ finding.confidence | capitalize }}

{% if not loop.last %}
---
{% endif %}
{% endfor %}